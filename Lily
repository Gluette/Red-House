做了一半才想着要写日志:D

[10/05/2022]
今日完成:
·人物随机脚步声：使用协程（IEnumerator）
        IEnumerator PlaySound()
        {
            while (true)
            {
                if (player.isGround && player.speed > 0.3f && ((player.rb.velocity.x) != 0))
                {
                    AudioClip acp = stepSound[Random.Range(0, stepSound.Length)];
                    audio1.PlayOneShot(acp);
                    yield return new WaitForSeconds(acp.length);
                }
                else
                {
                    yield return null;
                }
           
· 跟随某物移动：获取该物品坐标
public Transform obj;
    void Update()
    {
        transform.position =new Vector3(obj.transform.position.x-6,obj.transform.position.y+5,obj.transform.position.z);
    }
    
· 搭建了第二关初步框架

[12/05/2022] 
今日完成：
· 添加post-process后期处理
